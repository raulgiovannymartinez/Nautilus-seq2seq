apiVersion: v1
kind: Pod
metadata:
  name: dataaccess5
spec:
  containers:
  - name: mypod
    image: centos:centos7
    command: ["sh", "-c", "sleep infinity"]
    resources:
      limits:
        memory: 10Gi
      requests:
        memory: 10Gi
    volumeMounts:
    - mountPath: /models-vol5
      name: models-vol5
  volumes:
    - name: models-vol5
      persistentVolumeClaim:
        claimName: models-vol5